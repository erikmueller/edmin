<%= for slide <- @slides do %>
  <div class="swiper-slide">
    <%= if is_valid_video? slide.asset.file_name do %>
      <video poster="/images/test.png">
        <source
          src="/uploads/<%= slide.asset.file_name %>"
          type="video/<%= get_type slide.asset.file_name %>" />
      </video>
    <% else %>
      <img src="/uploads/<%= slide.asset.file_name %>" />
    <% end %>
  </div>
<% end %>

<script>
  window.__DURATIONS__ = <%= get_duration @slides %>
</script>
